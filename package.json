{
  "dependencies": {
    "firebase": "^10.12.1"
  },
  "scripts": {
    "deploy-functions": "npm run deploy --workspace=functions",
    "deploy": "npm run build --workspace=client && firebase deploy"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "workspaces": [
    "functions",
    "client"
  ]
}
